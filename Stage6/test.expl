type
  List
  {
    int data;
    List next;
  }
endtype

decl
    List head;
		int a[5];
enddecl

int main()
{
  decl
    int length, x;
    List p, q;
  enddecl

  begin
    p = alloc();
    free(p);
    initialize();
    if(head.next.next == head) then
		  write(p.next.next.next.data);
    endif;
    return 1;
  end
}
